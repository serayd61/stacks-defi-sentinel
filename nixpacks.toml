[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci --prefer-offline --no-audit"]

[phases.build]
cmds = ["NODE_ENV=production npm run build"]
# Environment variables are only needed at runtime, not during build

[start]
cmd = "npm start"

